package dev.yash.dynamicweatherapp.presentation.theme

import androidx.compose.ui.graphics.Color

// Base Backgrounds
val NimbusDark = Color(0xFF0B1221)      // The deep navy background
val NimbusSurface = Color(0xFF151D2D)   // A slightly lighter fallback surface

// Glassmorphism Colors (Translucent Whites)
val NimbusGlass = Color(0x1AFFFFFF)     // 10% Opacity White for card backgrounds
val NimbusGlassBorder = Color(0x33FFFFFF) // 20% Opacity White for subtle card borders

// Text & Accents
val NimbusTextWhite = Color(0xFFFFFFFF)
val NimbusTextHint = Color(0xFFA0A5BA)  // Muted grey/blue for secondary text ("15 min ago", etc.)
val NimbusAccentBlue = Color(0xFF8AB4F8) // For active toggles and highlights